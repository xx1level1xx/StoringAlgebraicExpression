//sameLevelArithmeticRelationshipBetweenNodes[numLevel][i]=0;
					//allTreePtrVar[i][col]->
					//node temp = new node();
					//temp.type = 
					//allTreePtr[numLevel][prevNumNextLevel + count2].prev.next = temp;
					//allTreePtr[numLevel][prevNumNextLevel + count2].prev = temp;
					//for (int k = 0; k < numNextLevelConnections[numLevel - 1][j]; k++){
					//	allTreePtr[numLevel][prevNumNextLevel + count2 + k].prev.next = temp;
					//	allTreePtr[numLevel][prevNumNextLevel + count2 + k].prev = temp;
					//}
